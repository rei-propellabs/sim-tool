export interface FileUpload {
  id: string,
  filename: string,
  originalFilename: string,
  mimeType: string,
  intension: string,
  isUploaded: boolean,
  uploadId: string,
  createdAt: string,
  updatedAt: string
}